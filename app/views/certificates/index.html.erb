<h1>Certificates</h1>

<%= link_to 'New Certificate', new_certificate_path, :class => 'btn btn-sm btn-success' if current_user.isAdmin? %>

<hr />

<% @certificates.each do |certificate| %>

    <div class="well col-sm-4" style="overflow-y:auto; height:190px">
        <h3><%= certificate.name %></h3>
        <p><%= certificate.description[0..30].gsub(/\s\w+\s*$/,'...') %></p>

        <%= link_to "Read More", certificate_path(certificate), :class => 'btn btn-secondary' %>

    </div>

<% end %>