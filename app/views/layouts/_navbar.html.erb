<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="row">
    
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapsible-content" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>

        <%#= link_to 'RubyProject', authenticated_root_path, :class => 'navbar-brand' %>

        <%= link_to_if(user_signed_in?, "Ruby Project", authenticated_root_path, {:class => 'navbar-brand'} ) { 
              link_to( "RubyProject", unauthenticated_root_path, :class => 'navbar-brand') 
        } %>
      </div>
 
      <div class="collapse navbar-collapse" id="navbar-collapsible-content">
        <% if user_signed_in? %>
          <ul class="nav navbar-nav">
            <% if current_user.isAdmin? %>
              <li class="nav-item">
                <%= link_to 'Users', users_path, :class => 'nav-link' %>
              </li>
            <% end %>
        
            <li class="nav-item">
              <%= link_to 'Certifications', certificates_path, :class => 'nav-item' %>
            </li>
          </ul>
        
          <ul class="nav navbar-nav navbar-right">
            <li class="dropdown pc-menu">
              <a id="user-settings" class="dropdown-toggle" data-toggle="dropdown" href="#">
                <span id="user-name"><%= current_user.first_name %></span>
                <span class="caret"></span>
              </a>

              <ul class="dropdown-menu" role="menu">
                <li><%= link_to 'Edit Profile', edit_user_registration_path %></li>
                <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
          </ul>
        <% end %>
      </div>
  
    </div>
</nav>